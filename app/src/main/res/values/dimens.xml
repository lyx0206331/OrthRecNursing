<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Because the window insets on round devices are larger than 1@dimen/inner_frame_layout_padding, this padding only applies
    to square screens.
    -->
    <dimen name="box_inset_layout_padding">0dp</dimen>

    <!--
    This padding applies to both square and round screens. The total padding between the buttons
    and the window insets is box_inset_layout_padding (above variable) on square screens and
    inner_frame_layout_padding (below variable) on round screens.
    -->
    <dimen name="inner_frame_layout_padding">5dp</dimen>

    <dimen name="titleText">19sp</dimen>

    <dimen name="dialogTitle">16sp</dimen>

    <dimen name="tutorialText">9sp</dimen>

    <dimen name="normal">12sp</dimen>

    <dimen name="big">17sp</dimen>

    <dimen name="titleMarginTop">30dp</dimen>

    <dimen name="lable_h">30dp</dimen>
</resources>